# GUIWindow

Container GUI window component

## Structure

Need to figure out how to render what's inside the component tags into window_content instead of passing the content as props

## Focus

Z-index adjustment is working on click. However, useEffect is attaching and removing the event listeners on initialization.

## Scrollbar

Add scrollbars for content that dips below the fold

## Resizing

Add a secondary, small resize box on click that will allow resizing only when the mouse is above it